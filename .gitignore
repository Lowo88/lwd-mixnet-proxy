/target
/state
.DS_Store

# Secrets and local artifacts — defense in depth; never commit these.
*.pem
*.key
.env
.env.*
# The template carries no values, only names and defaults, and is meant to be committed.
!.env.example
